INSTRUCTIONS:

~navigate to MusicRecommendation/

1. run 'npm start' or node server.js to start the server
2. run 'npm test' to run the script that makes API calls to the server

NOTE: It is important to run these commands while having MusicRecommendation as the active directory.
There are paths in test/test.js that require this
NOTE: npm test does not seem to work.

